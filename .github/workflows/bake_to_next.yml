name: Build next-tags

on:
  workflow_dispatch:
  push:
    branches:
      - next

jobs:
  bake-next:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    env:
      HAS_DOCKERHUB_CREDS: ${{ secrets.DOCKER_USERNAME != '' && secrets.DOCKER_PASSWORD != '' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: all
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to DockerHub
        if: env.HAS_DOCKERHUB_CREDS == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build (full) GHCR only
        if: env.HAS_DOCKERHUB_CREDS != 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/docker-py3-kms/Dockerfile
          platforms: linux/amd64,linux/386,linux/arm64/v8,linux/arm/v7,linux/arm/v6
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/py-kms:python3-next
          build-args: |
            BUILD_COMMIT=${{ github.sha }}
            BUILD_BRANCH=${{ github.ref_name }}
      - name: Build (full) DockerHub + GHCR
        if: env.HAS_DOCKERHUB_CREDS == 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/docker-py3-kms/Dockerfile
          platforms: linux/amd64,linux/386,linux/arm64/v8,linux/arm/v7,linux/arm/v6
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/py-kms:python3-next,ghcr.io/${{ github.repository_owner }}/py-kms:python3-next
          build-args: |
            BUILD_COMMIT=${{ github.sha }}
            BUILD_BRANCH=${{ github.ref_name }}
      - name: Build (minimal) GHCR only
        if: env.HAS_DOCKERHUB_CREDS != 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/docker-py3-kms-minimal/Dockerfile
          platforms: linux/amd64,linux/386,linux/arm64/v8,linux/arm/v7,linux/arm/v6
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/py-kms:latest-next,ghcr.io/${{ github.repository_owner }}/py-kms:minimal-next
          build-args: |
            BUILD_COMMIT=${{ github.sha }}
            BUILD_BRANCH=${{ github.ref_name }}
      - name: Build (minimal) DockerHub + GHCR
        if: env.HAS_DOCKERHUB_CREDS == 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/docker-py3-kms-minimal/Dockerfile
          platforms: linux/amd64,linux/386,linux/arm64/v8,linux/arm/v7,linux/arm/v6
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/py-kms:latest-next,ghcr.io/${{ github.repository_owner }}/py-kms:latest-next,${{ secrets.DOCKER_USERNAME }}/py-kms:minimal-next,ghcr.io/${{ github.repository_owner }}/py-kms:minimal-next
          build-args: |
            BUILD_COMMIT=${{ github.sha }}
            BUILD_BRANCH=${{ github.ref_name }}
